{% extends 'base.html.twig' %}

{% block title %}Mes commandes - TECH NOIR{% endblock %}

{% block body %}
<div class="container commandes-page">
    <h1>📦 Mes commandes</h1>

    {% if commandes is empty %}
        <p>Vous n'avez encore passé aucune commande.</p>
    {% else %}
        <table class="orders-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Date</th>
                    <th>Total</th>
                    <th>Détails</th>
                </tr>
            </thead>
            <tbody>
                {% for commande in commandes %}
                    <tr>
                        <td>#{{ commande.id }}</td>
                        <td>{{ commande.createdAt|date('d/m/Y H:i') }}</td>
                        <td>{{ commande.total|number_format(2, ',', ' ') }} €</td>
                        <td><a href="{{ path('commande_detail', { id: commande.id }) }}">🔍 Voir</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>
{% endblock %}