{% extends 'base.html.twig' %}

{% block title %}✅ Confirmation de commande - TECH NOIR{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('confirmation') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('confirmation') }}
{% endblock %}

{% block body %}
<div class="confirmation-page container">
    <div class="confirmation-card">
        <div class="icon-check">✅</div>
        <h1>Merci pour votre commande !</h1>

        <p>Votre commande a bien été enregistrée.</p>
        <p><strong>Numéro de commande :</strong> #{{ commande.id }}</p>
        <p><strong>Date :</strong> {{ commande.createdAt|date('d/m/Y H:i') }}</p>
        <p><strong>Total :</strong> {{ total|number_format(2, ',', ' ') }} €</p>

        <div class="confirmation-actions">
            <a href="{{ path('catalog_all') }}" class="btn btn-primary">⬅ Retour au catalogue</a>
            <a href="{{ path('profil_commandes') }}" class="btn btn-secondary">📦 Voir mes commandes</a>
        </div>
    </div>
</div>
{% endblock %}